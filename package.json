{
  "name": "catalog-seed",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node ./scripts/validate-catalog.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.5.1"
  }
}
